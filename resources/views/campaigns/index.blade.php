<x-layouts.app title="Kampanya Motoru"><section class="space-y-7"><header class="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p class="text-sm font-bold tracking-[.18em] text-orange-300">ÇOK KANALLI İLETİŞİM</p><h1 class="mt-3 text-4xl font-black">Kampanya Motoru</h1><p class="mt-2 text-slate-400">Hedef, kanal, içerik onayı ve yayın yaşam döngüsünü yönetin.</p></div>@can('create',App\Models\Campaign::class)<a href="{{ route('campaigns.create') }}" class="rounded-xl bg-orange-300 px-5 py-3 text-center font-bold text-slate-950">+ Kampanya oluştur</a>@endcan</header><form method="GET" class="flex flex-col gap-3 rounded-2xl border border-white/10 bg-white/[.04] p-4 sm:flex-row"><input name="q" value="{{ request('q') }}" placeholder="Kampanya ara" class="flex-1 rounded-xl border border-white/10 bg-slate-900 px-4 py-3"><select name="status" class="rounded-xl border border-white/10 bg-slate-900 px-4 py-3"><option value="">Tüm durumlar</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><button class="rounded-xl border border-orange-300/30 px-5 py-3 font-bold text-orange-200">Filtrele</button></form><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($campaigns as $campaign)<a href="{{ route('campaigns.show',$campaign) }}" class="rounded-2xl border border-white/10 bg-white/[.04] p-5 hover:border-orange-300/30"><div class="flex items-start justify-between gap-4"><div><h2 class="text-xl font-black">{{ $campaign->name }}</h2><p class="mt-1 text-xs text-slate-500">{{ $campaign->agency->name }} · {{ $campaign->owner?->name ?? 'Sistem' }}</p></div><span class="rounded-full bg-orange-400/10 px-3 py-1 text-xs font-bold text-orange-200">{{ $campaign->status->label() }}</span></div><p class="mt-4 line-clamp-2 text-sm text-slate-400">{{ $campaign->objective }}</p><div class="mt-5 flex flex-wrap gap-2">@foreach($campaign->channels as $channel)<span class="rounded-full bg-slate-800 px-3 py-1 text-xs">{{ App\CampaignChannel::from($channel)->label() }}</span>@endforeach</div><div class="mt-5 flex justify-between text-sm"><span class="text-slate-500">{{ $campaign->contents_count }} içerik</span><strong>{{ $campaign->budget ? number_format((float)$campaign->budget,2,',','.').' ₺' : 'Bütçe yok' }}</strong></div></a>@empty<div class="rounded-2xl border border-dashed border-white/15 p-14 text-center text-slate-400 md:col-span-2 xl:col-span-3">Henüz kampanya yok.</div>@endforelse</div>{{ $campaigns->links() }}</section></x-layouts.app>