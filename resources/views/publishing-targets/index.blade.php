<x-layouts.app title="Yayın Hedefleri"><section class="space-y-7"><header class="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p class="text-sm font-bold tracking-[.18em] text-emerald-300">WORDPRESS BAĞLANTILARI</p><h1 class="mt-3 text-4xl font-black">Yayın Hedefleri</h1><p class="mt-2 text-slate-400">Ajansların güvenli REST veya XML-RPC bağlantılarını yönetin.</p></div><a href="{{ route('publishing-targets.create') }}" class="rounded-xl bg-emerald-300 px-5 py-3 text-center font-bold text-slate-950">+ Hedef ekle</a></header><div class="grid gap-4 lg:grid-cols-2">@forelse($targets as $target)<article class="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div class="flex items-start justify-between gap-4"><div><h2 class="text-xl font-black">{{ $target->name }}</h2><p class="mt-1 text-sm text-slate-400">{{ $target->agency->name }} · {{ $target->protocol->label() }}</p></div><span class="rounded-full px-3 py-1 text-xs font-bold {{ $target->is_active ? 'bg-emerald-400/10 text-emerald-300' : 'bg-slate-400/10 text-slate-300' }}">{{ $target->is_active ? 'Aktif' : 'Pasif' }}</span></div><p class="mt-4 break-all text-sm text-cyan-300">{{ $target->base_url }}</p><div class="mt-4 grid grid-cols-2 gap-3 text-sm"><div class="rounded-xl bg-slate-900/70 p-3"><span class="text-slate-500">Yayın</span><strong class="block text-lg">{{ $target->publications_count }}</strong></div><div class="rounded-xl bg-slate-900/70 p-3"><span class="text-slate-500">Son bağlantı</span><strong class="block text-sm">{{ $target->last_connected_at?->format('d.m.Y H:i') ?? 'Henüz yok' }}</strong></div></div>@if($target->last_error)<p class="mt-4 rounded-xl bg-rose-400/10 p-3 text-sm text-rose-200">{{ Str::limit($target->last_error, 180) }}</p>@endif<div class="mt-5 flex gap-2"><a href="{{ route('publishing-targets.edit', $target) }}" class="rounded-lg border border-white/10 px-4 py-2 text-sm font-bold hover:bg-white/5">Düzenle</a><form method="POST" action="{{ route('publishing-targets.destroy', $target) }}" onsubmit="return confirm('Bu hedef silinsin mi?')">@csrf @method('DELETE')<button class="rounded-lg border border-rose-400/20 px-4 py-2 text-sm font-bold text-rose-300">Sil</button></form></div></article>@empty<div class="rounded-2xl border border-dashed border-white/15 p-12 text-center text-slate-400 lg:col-span-2">Henüz yayın hedefi bulunmuyor.</div>@endforelse</div>{{ $targets->links() }}</section></x-layouts.app>