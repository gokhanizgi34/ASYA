<x-layouts.app title="Plan Ekle"><section class="mx-auto max-w-4xl space-y-7"><header><p class="text-sm font-bold tracking-[.18em] text-sky-300">ZAMANLAYICI</p><h1 class="mt-3 text-4xl font-black">Yeni plan</h1><p class="mt-2 text-slate-400">Başarısız WordPress yayınını yeniden deneyin veya hazır kampanyanın durumunu otomatik değiştirin.</p></header><form method="POST" action="{{ route('schedules.store') }}" class="space-y-6 rounded-2xl border border-white/10 bg-white/[.04] p-6">@csrf<div class="grid gap-5 lg:grid-cols-2"><label class="space-y-2"><span class="text-sm font-bold">Ajans</span><select name="agency_id" class="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3" @disabled(! auth()->user()->isSystemAdministrator())>@foreach($agencies as $agency)<option value="{{ $agency->id }}" @selected((string)old('agency_id',auth()->user()->agency_id)===(string)$agency->id)>{{ $agency->name }}</option>@endforeach</select>@if(!auth()->user()->isSystemAdministrator())<input type="hidden" name="agency_id" value="{{ auth()->user()->agency_id }}">@endif</label><label class="space-y-2"><span class="text-sm font-bold">Eylem</span><select name="action" required class="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3">@foreach($actions as $action)<option value="{{ $action->value }}" @selected(old('action')===$action->value)>{{ $action->label() }}</option>@endforeach</select></label><label class="space-y-2 lg:col-span-2"><span class="text-sm font-bold">Başarısız WordPress yayını</span><select name="publication_id" class="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3"><option value="">Yayın eylemi değilse boş bırakın</option>@foreach($publications as $publication)<option value="{{ $publication->id }}" @selected((string)old('publication_id')===(string)$publication->id)>{{ $publication->article->title }} → {{ $publication->publishingTarget->name }}</option>@endforeach</select></label><label class="space-y-2 lg:col-span-2"><span class="text-sm font-bold">Kampanya</span><select name="campaign_id" class="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3"><option value="">WordPress eylemiyse boş bırakın</option>@foreach($campaigns as $campaign)<option value="{{ $campaign->id }}" @selected((string)old('campaign_id')===(string)$campaign->id)>{{ $campaign->name }} · {{ $campaign->status->label() }}</option>@endforeach</select></label><label class="space-y-2"><span class="text-sm font-bold">Tarih ve saat</span><input type="datetime-local" name="scheduled_for" required value="{{ old('scheduled_for',now()->addHour()->format('Y-m-d\TH:i')) }}" class="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3"></label><label class="space-y-2"><span class="text-sm font-bold">Saat dilimi</span><select name="timezone" class="w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3"><option value="Europe/Istanbul">Europe/Istanbul</option><option value="UTC">UTC</option></select></label></div><div class="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><a href="{{ route('schedules.index') }}" class="rounded-xl border border-white/10 px-5 py-3 text-center font-bold">Vazgeç</a><button class="rounded-xl bg-sky-300 px-6 py-3 font-bold text-slate-950">Takvime ekle</button></div></form></section></x-layouts.app>