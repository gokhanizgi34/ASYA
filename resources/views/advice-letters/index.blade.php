<x-layouts.app title="Mukaddes Abla"><section class="space-y-7"><header class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-bold tracking-[.18em] text-fuchsia-300">MUKADDES ABLA</p><h1 class="mt-3 text-4xl font-black">Mukaddes Abla</h1><p class="mt-2 text-slate-400">Takma adlı mektupları mahremiyet ve güvenlik taramasıyla yönetin.</p></div><a href="{{ route('advice-letters.create') }}" class="rounded-xl bg-fuchsia-300 px-5 py-3 text-center font-black text-slate-950">Mektup ekle</a></header>
<form method="GET" class="grid gap-3 rounded-2xl border border-white/10 bg-white/[.03] p-4 md:grid-cols-4"><select name="status" class="rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5"><option value="">Tüm durumlar</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><select name="risk" class="rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5"><option value="">Tüm riskler</option>@foreach($risks as $risk)<option value="{{ $risk->value }}" @selected(request('risk')===$risk->value)>{{ $risk->label() }}</option>@endforeach</select><select name="category" class="rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5"><option value="">Tüm kategoriler</option>@foreach($categories as $key=>$label)<option value="{{ $key }}" @selected(request('category')===$key)>{{ $label }}</option>@endforeach</select><button class="rounded-xl border border-fuchsia-400/30 px-4 py-2.5 text-fuchsia-300">Filtrele</button></form>
<div class="grid gap-4 lg:grid-cols-2">@forelse($letters as $letter)<article class="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div class="flex justify-between gap-4"><div><h2 class="text-xl font-black">{{ $letter->pseudonym }}</h2><p class="text-sm text-slate-500">{{ $categories[$letter->category] }} · {{ $letter->agency->name }}</p></div><div class="text-right"><span class="block text-sm">{{ $letter->status->label() }}</span><span class="text-xs {{ $letter->risk_level===App\AdviceRiskLevel::Critical?'text-rose-300':'text-amber-300' }}">{{ $letter->risk_level->label() }} risk</span></div></div><p class="mt-4 line-clamp-3 text-sm text-slate-300">{{ $letter->question }}</p><div class="mt-5 flex justify-end gap-2"><a href="{{ route('advice-letters.show',$letter) }}" class="rounded-lg border border-white/10 px-3 py-2 text-sm">İncele</a>@can('update',$letter)<a href="{{ route('advice-letters.edit',$letter) }}" class="rounded-lg border border-fuchsia-400/20 px-3 py-2 text-sm text-fuchsia-300">Yanıtla</a>@endcan</div></article>@empty<div class="rounded-2xl border border-dashed border-white/15 p-14 text-center text-slate-400 lg:col-span-2">Henüz mektup bulunmuyor.</div>@endforelse</div>{{ $letters->links() }}</section></x-layouts.app>
