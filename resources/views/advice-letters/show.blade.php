<x-layouts.app title="Danışma Mektubu"><section class="mx-auto max-w-5xl space-y-6"><header class="flex flex-col justify-between gap-4 sm:flex-row"><div><a href="{{ route('advice-letters.index') }}" class="text-fuchsia-300">← Mukaddes Abla</a><h1 class="mt-4 text-3xl font-black">{{ $letter->pseudonym }}</h1><p class="text-slate-500">{{ $categories[$letter->category] }} · {{ $letter->agency->name }} · {{ $letter->status->label() }}</p></div>@can('update',$letter)<a href="{{ route('advice-letters.edit',$letter) }}" class="self-start rounded-xl bg-fuchsia-300 px-5 py-3 font-black text-slate-950">Editoryal işlem</a>@endcan</header><div class="grid gap-5 lg:grid-cols-[1fr_280px]"><article class="rounded-2xl border border-white/10 bg-white/[.03] p-6"><h2 class="text-lg font-black">Mektup</h2><p class="mt-4 whitespace-pre-wrap text-slate-300">{{ $letter->question }}</p></article><aside class="rounded-2xl border border-white/10 p-5"><h2 class="font-black">Güvenlik taraması</h2><p class="mt-3 text-xl font-black {{ $letter->risk_level===App\AdviceRiskLevel::Critical?'text-rose-300':'text-amber-300' }}">{{ $letter->risk_level->label() }}</p><div class="mt-3 flex flex-wrap gap-2">@forelse($letter->risk_flags as $flag)<span class="rounded-full bg-rose-400/10 px-2 py-1 text-xs text-rose-300">{{ $flag }}</span>@empty<span class="text-sm text-emerald-300">İşaret bulunmadı</span>@endforelse</div></aside></div>@if($letter->response_body)<article class="rounded-2xl border border-fuchsia-400/20 bg-fuchsia-400/[.04] p-6"><h2 class="text-2xl font-black">{{ $letter->response_title }}</h2><p class="mt-4 whitespace-pre-wrap text-slate-200">{{ $letter->response_body }}</p><small class="mt-5 block text-slate-500">{{ $letter->answerer?->name }} · {{ $letter->answered_at?->format('d.m.Y H:i') }}</small></article>@endif</section></x-layouts.app>
