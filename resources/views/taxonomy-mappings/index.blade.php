<x-layouts.app title="Kategori ve Etiket Eşleştirme"><section class="space-y-7"><header class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-bold tracking-[.18em] text-cyan-300">YAYIN TAKSONOMİSİ</p><h1 class="mt-3 text-4xl font-black">Kategori ve Etiket Eşleştirme</h1><p class="mt-2 text-slate-400">SEO terimlerini her yayın hedefindeki kategori ve etiket kimliklerine otomatik dönüştürün.</p></div><a href="{{ route('taxonomy-mappings.create') }}" class="rounded-xl bg-cyan-400 px-5 py-3 text-center font-black text-slate-950">Yeni eşleştirme</a></header><div class="overflow-hidden rounded-2xl border border-white/10"><div class="divide-y divide-white/10">@forelse($mappings as $mapping)<article class="grid gap-3 bg-white/[.03] p-5 lg:grid-cols-[100px_1fr_1fr_120px_auto] lg:items-center"><span class="w-fit rounded-full bg-violet-400/15 px-2.5 py-1 text-xs text-violet-300">{{ $mapping->type->label() }}</span><div><strong>{{ $mapping->source_term }}</strong><small class="block text-slate-500">{{ $mapping->agency->name }}</small></div><div><strong>#{{ $mapping->remote_id }} · {{ $mapping->remote_name }}</strong><small class="block text-slate-500">{{ $mapping->publishingTarget->name }}</small></div><span class="text-sm">Öncelik {{ $mapping->priority }}</span><div class="flex gap-2"><a href="{{ route('taxonomy-mappings.edit',$mapping) }}" class="rounded-lg border border-white/10 px-3 py-2 text-sm">Düzenle</a><form method="POST" action="{{ route('taxonomy-mappings.destroy',$mapping) }}">@csrf @method('DELETE')<button class="rounded-lg border border-rose-400/20 px-3 py-2 text-sm text-rose-300">Sil</button></form></div></article>@empty<div class="p-14 text-center text-slate-400">Henüz eşleştirme yok.</div>@endforelse</div></div>{{ $mappings->links() }}</section></x-layouts.app>
