<x-layouts.app title="Köşe Taslağı"><section class="mx-auto max-w-5xl space-y-6"><a href="{{ route('ai-columnists.index') }}" class="text-teal-300">← AI Yazarlar</a><header><p class="text-sm text-slate-500">{{ $draft->columnist->pen_name }} · {{ $draft->status->label() }}</p><h1 class="text-3xl font-black">{{ $draft->headline }}</h1></header><form method="POST" action="{{ route('columnist-drafts.update',$draft) }}" class="space-y-5 rounded-2xl border border-white/10 p-6">@csrf @method('PUT')<label class="block">Başlık<input name="headline" value="{{ old('headline',$draft->headline) }}" required class="mt-1 w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5"></label><label class="block">Metin<textarea name="body" rows="18" required class="mt-1 w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">{{ old('body',$draft->body) }}</textarea></label><label class="block">Durum<select name="status" class="mt-1 w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">@foreach($statuses as $s)<option value="{{ $s->value }}" @selected(old('status',$draft->status->value)===$s->value)>{{ $s->label() }}</option>@endforeach</select></label><button class="rounded-xl bg-teal-300 px-6 py-3 font-black text-slate-950">Kaydet</button></form></section></x-layouts.app>
