<x-layouts.app title="{{ $topic->name }} Trendi"><section class="mx-auto max-w-6xl space-y-7"><header class="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p class="text-sm font-bold tracking-[.18em] text-fuchsia-300">TREND DETAYI</p><h1 class="mt-3 text-4xl font-black">{{ $topic->name }}</h1><p class="mt-2 text-slate-400">{{ $topic->agency->name }} · İlk sinyal {{ $topic->first_seen_at->format('d.m.Y H:i') }}</p></div><a href="{{ route('trends.index') }}" class="rounded-xl border border-white/10 px-5 py-3 text-center font-bold">Trendlere dön</a></header><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach([['Durum',$topic->status->label()],['Trend puanı',number_format($topic->score,1)],['Sinyal / Kaynak',$topic->mention_count.' / '.$topic->source_count],['Değişim',($topic->velocity >= 0 ? '+' : '').number_format($topic->velocity,1).'%']] as [$label,$value])<div class="rounded-2xl border border-white/10 bg-white/[.04] p-5"><span class="text-sm text-slate-500">{{ $label }}</span><strong class="mt-2 block text-2xl">{{ $value }}</strong></div>@endforeach</div><article class="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div class="flex items-center justify-between"><div><h2 class="text-xl font-black">15 dakikalık zaman serisi</h2><p class="mt-1 text-sm text-slate-500">Son {{ $snapshots->count() }} ölçüm</p></div></div>@php($maxScore = max(1, (float) $snapshots->max('score')))<div class="mt-6 flex h-56 items-end gap-1 overflow-x-auto border-b border-white/10 pb-1" role="img" aria-label="Trend puanı zaman serisi">@forelse($snapshots as $snapshot)<div class="min-w-3 flex-1 rounded-t bg-gradient-to-t from-fuchsia-700 to-fuchsia-300" style="height: {{ max(4, ($snapshot->score / $maxScore) * 100) }}%" title="{{ $snapshot->period_end->format('d.m H:i') }} · {{ number_format($snapshot->score,1) }} puan"></div>@empty<p class="m-auto text-slate-500">Zaman serisi henüz oluşmadı.</p>@endforelse</div></article><div class="grid gap-5 lg:grid-cols-2"><article class="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h2 class="text-xl font-black">Örnek haberler</h2><ul class="mt-4 space-y-3">@forelse(data_get($topic->context,'examples',[]) as $example)<li class="rounded-xl bg-slate-900/70 p-3 text-sm">{{ $example }}</li>@empty<li class="text-sm text-slate-500">Başlık örneği bulunmuyor.</li>@endforelse</ul></article><article class="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h2 class="text-xl font-black">Sinyal kaynakları</h2><div class="mt-4 flex flex-wrap gap-2">@forelse(data_get($topic->context,'sources',[]) as $source)<span class="rounded-full bg-cyan-400/10 px-3 py-1 text-sm text-cyan-200">{{ $source }}</span>@empty<span class="text-sm text-slate-500">Kaynak bilgisi bulunmuyor.</span>@endforelse</div></article></div></section></x-layouts.app>