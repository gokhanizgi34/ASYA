<div class="grid gap-5 lg:grid-cols-2">
    <label><span class="mb-1 block text-sm font-semibold">Ajans</span><select name="agency_id" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">@foreach($agencies as $agency)<option value="{{ $agency->id }}" @selected((string) old('agency_id', $rule?->agency_id) === (string) $agency->id)>{{ $agency->name }}</option>@endforeach</select></label>
    <label><span class="mb-1 block text-sm font-semibold">Kural türü</span><select name="type" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">@foreach($types as $type)<option value="{{ $type->value }}" @selected(old('type', $rule?->type?->value) === $type->value)>{{ $type->label() }}</option>@endforeach</select></label>
    <label class="lg:col-span-2"><span class="mb-1 block text-sm font-semibold">Kalıp</span><input name="pattern" value="{{ old('pattern', $rule?->pattern) }}" minlength="2" maxlength="1000" required placeholder="Örn. şüpheli-kaynak.example" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5"><small class="mt-1 block text-slate-500">Alan adı için sadece alan adını; URL için http:// veya https:// ile başlayan adresi girin.</small></label>
    <label><span class="mb-1 block text-sm font-semibold">Eşleşince</span><select name="action" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">@foreach($actions as $action)<option value="{{ $action->value }}" @selected(old('action', $rule?->action?->value) === $action->value)>{{ $action->label() }}</option>@endforeach</select></label>
    <div class="flex items-end"><input type="hidden" name="is_active" value="0"><label class="inline-flex items-center gap-3 rounded-xl border border-white/10 px-4 py-2.5"><input type="checkbox" name="is_active" value="1" @checked((bool) old('is_active', $rule?->is_active ?? true))><span>Etkin</span></label></div>
    <label class="lg:col-span-2"><span class="mb-1 block text-sm font-semibold">Gerekçe</span><textarea name="reason" rows="3" maxlength="500" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">{{ old('reason', $rule?->reason) }}</textarea></label>
</div>
