<x-layouts.app title="Plan #{{ $entry->id }}"><section class="mx-auto max-w-4xl space-y-7"><header class="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p class="text-sm font-bold tracking-[.18em] text-sky-300">PLAN #{{ $entry->id }}</p><h1 class="mt-3 text-3xl font-black">{{ $entry->title }}</h1><p class="mt-2 text-slate-400">{{ $entry->action->label() }}</p></div><a href="{{ route('schedules.index') }}" class="rounded-xl border border-white/10 px-5 py-3 text-center font-bold">Takvime dön</a></header><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach([['Durum',$entry->status->label()],['Plan zamanı',$entry->scheduled_for->format('d.m.Y H:i')],['Saat dilimi',$entry->timezone],['Deneme',$entry->attempt_count]] as [$label,$value])<div class="rounded-2xl border border-white/10 bg-white/[.04] p-5"><span class="text-sm text-slate-500">{{ $label }}</span><strong class="mt-2 block text-lg">{{ $value }}</strong></div>@endforeach</div>@if($entry->failure_message)<div class="rounded-2xl border border-rose-400/20 bg-rose-400/10 p-5"><h2 class="font-black text-rose-200">Yürütme hatası</h2><p class="mt-2 text-sm text-rose-100">{{ $entry->failure_message }}</p></div>@endif<article class="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h2 class="text-xl font-black">Kaynak</h2>@if($entry->publication)<p class="mt-3">{{ $entry->publication->article->title }}</p><p class="mt-1 text-sm text-slate-500">{{ $entry->publication->publishingTarget->name }}</p><a href="{{ route('publications.show',$entry->publication) }}" class="mt-4 inline-block text-cyan-300">Yayın kaydını aç</a>@elseif($entry->campaign)<p class="mt-3">{{ $entry->campaign->name }}</p><a href="{{ route('campaigns.show',$entry->campaign) }}" class="mt-4 inline-block text-cyan-300">Kampanyayı aç</a>@endif</article>@can('update',$entry)<div class="flex flex-wrap gap-3">@if($entry->status===App\ScheduleStatus::Failed)<form method="POST" action="{{ route('schedules.status',$entry) }}">@csrf @method('PATCH')<input type="hidden" name="operation" value="retry"><button class="rounded-xl bg-sky-300 px-5 py-3 font-bold text-slate-950">Şimdi yeniden dene</button></form>@endif @if(in_array($entry->status,[App\ScheduleStatus::Pending,App\ScheduleStatus::Failed],true))<form method="POST" action="{{ route('schedules.status',$entry) }}">@csrf @method('PATCH')<input type="hidden" name="operation" value="cancel"><button class="rounded-xl border border-rose-400/20 px-5 py-3 font-bold text-rose-300">Planı iptal et</button></form>@endif</div>@endcan</section></x-layouts.app>