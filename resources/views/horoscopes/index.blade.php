<x-layouts.app title="Günlük Burçlar"><section class="space-y-7"><header><p class="text-sm font-bold tracking-[.18em] text-indigo-300">ASTROLOJİ EDİTÖRÜ</p><h1 class="mt-3 text-4xl font-black">Günlük Burçlar</h1><p class="mt-2 text-slate-400">On iki burcun günlük editoryal yorumlarını ajans bazında hazırlayın.</p></header><div class="grid gap-4 lg:grid-cols-[1fr_auto]"><form method="GET" class="flex gap-3"><input type="date" name="date" value="{{ $date }}" class="flex-1 rounded-xl border border-white/10 bg-slate-900 px-4 py-3"><button class="rounded-xl border border-indigo-400/30 px-5 py-3 text-indigo-300">Tarihi aç</button></form><form method="POST" action="{{ route('horoscopes.day') }}" class="flex flex-col gap-2 sm:flex-row">@csrf<select name="agency_id" class="rounded-xl border border-white/10 bg-slate-900 px-4 py-3">@foreach($agencies as $agency)<option value="{{ $agency->id }}">{{ $agency->name }}</option>@endforeach</select><input type="hidden" name="forecast_date" value="{{ $date }}"><button class="rounded-xl bg-indigo-300 px-5 py-3 font-black text-slate-950">12 taslağı hazırla</button></form></div><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">@foreach($signs as $sign)@php($forecast=$forecasts->get($sign->value))<article class="rounded-2xl border {{ $forecast ? 'border-white/10 bg-white/[.03]' : 'border-dashed border-white/15' }} p-5"><div class="flex justify-between"><h2 class="text-xl font-black">{{ $sign->label() }}</h2>@if($forecast)<span class="{{ $forecast->status===App\HoroscopeStatus::Published?'text-emerald-300':'text-amber-300' }}">{{ $forecast->status->label() }}</span>@endif</div>@if($forecast)<p class="mt-4 line-clamp-4 text-sm text-slate-300">{{ $forecast->general ?: 'Editoryal yorum bekliyor.' }}</p><div class="mt-5 flex justify-between text-xs text-slate-500"><span>{{ $forecast->lucky_color ?: 'Renk —' }} · {{ $forecast->lucky_number ?: '—' }}</span><a href="{{ route('horoscopes.edit',$forecast) }}" class="text-indigo-300">Düzenle</a></div>@else<p class="mt-4 text-sm text-slate-500">Bu tarih için taslak yok.</p>@endif</article>@endforeach</div></section></x-layouts.app>
