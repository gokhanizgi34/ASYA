<x-layouts.app title="AI Haber Üretimi">
    <section class="space-y-7">
        <header class="flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p class="text-sm font-bold tracking-[.18em] text-violet-300">OTONOM ÜRETİM BANDI</p><h1 class="mt-3 text-4xl font-black">AI Haber Üretimi</h1><p class="mt-2 text-slate-400">Ham haberleri kategori promptlarıyla toplu işleyin ve her üretim öğesini uçtan uca izleyin.</p></div><a href="{{ route('content-batches.create') }}" class="rounded-xl bg-violet-300 px-5 py-3 text-center font-bold text-slate-950 hover:bg-violet-200">+ Üretim bandı oluştur</a></header>
        <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">@foreach($statuses as $status)<a href="{{ route('content-batches.index', ['status' => $status->value]) }}" class="rounded-xl border p-4 {{ request('status') === $status->value ? 'border-violet-300/50 bg-violet-300/10' : 'border-white/10 bg-white/[.04]' }}"><span class="text-xs text-slate-400">{{ $status->label() }}</span><strong class="mt-2 block text-2xl">{{ $statusCounts[$status->value] }}</strong></a>@endforeach</div>
        <form method="GET" action="{{ route('content-batches.index') }}" class="grid gap-3 rounded-2xl border border-white/10 bg-white/[.04] p-4 sm:grid-cols-[1fr_220px_auto]"><input name="q" value="{{ request('q') }}" placeholder="Üretim bandı ara" class="rounded-xl border border-white/10 bg-slate-900 px-4 py-3 outline-none focus:border-violet-300"><select name="status" class="rounded-xl border border-white/10 bg-slate-900 px-4 py-3 outline-none focus:border-violet-300"><option value="">Tüm durumlar</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->label() }}</option>@endforeach</select><button class="rounded-xl border border-violet-300/30 px-5 py-3 font-bold text-violet-200 hover:bg-violet-300/10">Filtrele</button></form>
        <div class="overflow-hidden rounded-2xl border border-white/10 bg-white/[.04]"><div class="overflow-x-auto"><table class="w-full text-left"><thead class="border-b border-white/10 bg-white/[.03] text-xs uppercase tracking-wider text-slate-400"><tr><th class="px-5 py-4">Üretim bandı</th><th class="px-5 py-4">Prompt</th><th class="px-5 py-4">İlerleme</th><th class="px-5 py-4">Durum</th><th class="px-5 py-4 text-right">İşlem</th></tr></thead><tbody class="divide-y divide-white/10">@forelse($batches as $batch)<tr><td class="px-5 py-4"><a href="{{ route('content-batches.show', $batch) }}" class="font-black hover:text-violet-300">{{ $batch->name }}</a><span class="mt-1 block text-xs text-slate-500">{{ $batch->agency->name }} · {{ $batch->creator?->name ?? 'Otomasyon' }}</span></td><td class="px-5 py-4 text-sm"><strong>{{ $batch->aiPrompt->name }}</strong><span class="mt-1 block text-xs text-slate-500">v{{ data_get($batch->settings, 'prompt_snapshot.version', $batch->aiPrompt->version) }}</span></td><td class="min-w-52 px-5 py-4"><div class="flex justify-between text-xs text-slate-400"><span>{{ $batch->processed_items }} başarılı</span><span>{{ $batch->failed_items }} hatalı</span></div><div class="mt-2 h-2 overflow-hidden rounded-full bg-slate-800"><div class="h-full rounded-full bg-violet-300" style="width: {{ $batch->progressPercentage() }}%"></div></div></td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ match($batch->status) { App\ContentBatchStatus::Completed => 'bg-emerald-400/10 text-emerald-300', App\ContentBatchStatus::Failed => 'bg-rose-400/10 text-rose-300', App\ContentBatchStatus::Partial => 'bg-amber-400/10 text-amber-300', App\ContentBatchStatus::Processing => 'bg-cyan-400/10 text-cyan-300', default => 'bg-slate-400/10 text-slate-300' } }}">{{ $batch->status->label() }}</span></td><td class="px-5 py-4 text-right"><div class="flex justify-end gap-2"><a href="{{ route('content-batches.show', $batch) }}" class="rounded-lg border border-white/10 px-3 py-2 text-sm hover:bg-white/5">İncele</a>@can('update',$batch)<a href="{{ route('content-batches.edit',$batch) }}" class="rounded-lg border border-violet-400/20 px-3 py-2 text-sm text-violet-300">Düzenle</a>@endcan</div></td></tr>@empty<tr><td colspan="5" class="px-5 py-14 text-center text-slate-400">Henüz üretim bandı bulunmuyor.</td></tr>@endforelse</tbody></table></div>@if($batches->hasPages())<div class="border-t border-white/10 px-5 py-4">{{ $batches->links() }}</div>@endif</div>
    </section>
</x-layouts.app>