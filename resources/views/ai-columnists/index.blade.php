<x-layouts.app title="AI Köşe Yazarları"><section class="space-y-8"><header class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-sm font-bold tracking-[.18em] text-teal-300">EDİTORYAL KİMLİKLER</p><h1 class="mt-3 text-4xl font-black">AI Köşe Yazarları</h1><p class="mt-2 text-slate-400">Üslup, uzmanlık, prompt ve zorunlu AI açıklamasıyla sanal yazar profilleri.</p></div><div class="flex gap-2">@can('create',App\Models\AiColumnist::class)<a href="{{ route('ai-columnists.create') }}" class="rounded-xl border border-teal-400/30 px-5 py-3 text-teal-300">Yazar ekle</a>@endcan<a href="{{ route('columnist-drafts.create') }}" class="rounded-xl bg-teal-300 px-5 py-3 font-black text-slate-950">Taslak oluştur</a></div></header><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($columnists as $c)<article class="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div class="flex justify-between"><h2 class="text-xl font-black">{{ $c->pen_name }}</h2><span class="{{ $c->is_active?'text-emerald-300':'text-slate-500' }}">{{ $c->is_active?'Aktif':'Pasif' }}</span></div><p class="text-sm text-slate-500">{{ $c->agency->name }} · {{ implode(', ',$c->expertise) }}</p><p class="mt-4 line-clamp-3 text-sm text-slate-300">{{ $c->biography }}</p>@can('update',$c)<a href="{{ route('ai-columnists.edit',$c) }}" class="mt-4 inline-block text-sm text-teal-300">Profili düzenle</a>@endcan</article>@empty<p class="text-slate-500">Henüz yazar yok.</p>@endforelse</div><div><h2 class="mb-4 text-2xl font-black">Son taslaklar</h2><div class="divide-y divide-white/10 rounded-2xl border border-white/10">@forelse($drafts as $d)<a href="{{ route('columnist-drafts.show',$d) }}" class="flex justify-between gap-4 p-4 hover:bg-white/[.03]"><span><strong>{{ $d->headline }}</strong><small class="block text-slate-500">{{ $d->columnist->pen_name }}</small></span><span>{{ $d->status->label() }}</span></a>@empty<p class="p-8 text-center text-slate-500">Taslak yok.</p>@endforelse</div></div></section></x-layouts.app>
