<x-layouts.app title="Danışma Yanıtı"><section class="mx-auto max-w-5xl space-y-6"><a href="{{ route('advice-letters.show',$letter) }}" class="text-fuchsia-300">← Mektuba dön</a><h1 class="text-3xl font-black">{{ $letter->pseudonym }} için editoryal yanıt</h1>@if($letter->risk_level===App\AdviceRiskLevel::Critical)<div class="rounded-xl border border-rose-400/30 bg-rose-400/10 p-4 text-rose-100">Kritik güvenlik işareti vardır. Yanıt/yayın engellidir; uzman incelemesi veya reddetme seçilmelidir.</div>@endif<form method="POST" action="{{ route('advice-letters.update',$letter) }}" class="space-y-5 rounded-2xl border border-white/10 bg-white/[.04] p-6">@csrf @method('PUT')<label class="block"><span class="mb-1 block font-semibold">Durum</span><select name="status" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(old('status',$letter->status->value)===$status->value)>{{ $status->label() }}</option>@endforeach</select></label><label class="block"><span class="mb-1 block font-semibold">Yanıt başlığı</span><input name="response_title" value="{{ old('response_title',$letter->response_title) }}" maxlength="180" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5"></label><label class="block"><span class="mb-1 block font-semibold">Yanıt</span><textarea name="response_body" rows="14" maxlength="10000" class="w-full rounded-xl border border-white/10 bg-slate-900 px-3 py-2.5">{{ old('response_body',$letter->response_body) }}</textarea></label><button class="rounded-xl bg-fuchsia-300 px-6 py-3 font-black text-slate-950">Kaydet</button></form></section></x-layouts.app>
